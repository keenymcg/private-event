<%# Display a list of all the events %>

<h1>Events</h1>

<% @events.each do |event| %>
    <p><%= event.name %></p>
<% end %>

<%# Add a link to the new event page %>
<%= link_to 'New Event', new_event_path %>